# Server-only GitHub token used to render the contribution calendar.
# Grant the minimum profile access required for anonymized private contribution counts.
GITHUB_TOKEN=your_github_personal_access_token_here
